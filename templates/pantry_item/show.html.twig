{% extends 'base.html.twig' %}

{% block title %}PantryItem{% endblock %}

{% block body %}
    <h1>PantryItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pantry_item.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pantry_item.name }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ pantry_item.quantity }}</td>
            </tr>
            <tr>
                <th>Measure</th>
                <td>{{ pantry_item.measure }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pantry_item_index') }}">back to list</a>

    <a href="{{ path('app_pantry_item_edit', {'id': pantry_item.id}) }}">edit</a>

    {{ include('pantry_item/_delete_form.html.twig') }}
{% endblock %}
